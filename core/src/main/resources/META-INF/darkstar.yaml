---
http:

  connectors:

    http: 8000

    https:
      port: 8443
      #tls:
      #  cert: core/src/test/resources/example.com.crt.pem
      #  key: core/src/test/resources/example.com.key.pem

  vhosts:

    '*.foo.com':

      origin: whatever.com

      #tls:
      #  cert: core/src/test/resources/example.com.crt.pem
      #  key: core/src/test/resources/example.com.key.pem

      #routes:
      #  /: localhost:8080